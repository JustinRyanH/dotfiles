[alias]
  poop = !git submodule sync && git submodule update --init --recursive
	rpull = pull --rebase
	st = status
  lg = log --color --graph --pretty=format:'%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset' --abbrev-commit
  rsst = reset --hard origin/master
